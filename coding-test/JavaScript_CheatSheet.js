/*** 남은시간 활용법! ***/
//  * 모든 문제를 다시 한번 꼼꼼히 읽기! (문제에는 내가 놓친 힌트/답이 있다!!)
//  * 히든케이스 찾기!

// 출력
console.log();

// ★이진법으로 변환하기
(4).toString(2);  // '100' 반환


/* 배열 메소드 */
splice(시작점, 삭제개수, 제거한 위치에 삽입할 요소(들));
slice(시작점, 끝점(미포함));
filter((v, i) => v != 0);
shift     // 맨 앞 요소 제거하면서 반환
unshift   // 요소를 앞에 추가
includes  // true, false로 리턴
arr.indexOf(elem);    // 존재 시 index 반환, 없으면 -1


/* String 메소드 */
substr(시작점, 길이);
substring(시작점, 미포함끝점);
slice(시작점, 미포함끝점);
문자열.repeat(숫자);
​

